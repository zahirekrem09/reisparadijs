import {getAccessToken,logout,checkAuth} from "../../js/auth/auth.js";

document.addEventListener('DOMContentLoaded', function () {
    // checkAuth();
    const userDetails = document.getElementById('userDetails');
});
